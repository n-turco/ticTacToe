<Window x:Class="ticTacToe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ticTacToe"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.New"
                        Executed="NewCommand_Executed"/>
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <!--MenuBar-->
            <RowDefinition Height="Auto" />
            <!-- Game board -->
            <RowDefinition Height="*" />
            <!-- Status bar -->
            <RowDefinition Height="Auto" />
            
        </Grid.RowDefinitions>
        <!--MenuBar layout-->
        <Menu x:Name="MenuBar" 
            Grid.Row="0"
            >
            <MenuItem Header="Menu">
                <MenuItem Header="_New Game"
                          Command="ApplicationCommands.New"/>
                <MenuItem Header="E_xit Game"/>
                <MenuItem Header="_Change player Name"/>
            </MenuItem>
        </Menu>
        <!-- Tic Tac Toe Board -->
        <Grid x:Name="GameBoard" Grid.Row="1">
            
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <!-- Top -->
            <Button x:Name="TopLeft" Grid.Row="0" Grid.Column="0" FontSize="32" Click="Cell_Click"/>
            <Button x:Name="TopCenter" Grid.Row="0" Grid.Column="1" FontSize="32" Click="Cell_Click"/>
            <Button x:Name="TopRight" Grid.Row="0" Grid.Column="2" FontSize="32" Click="Cell_Click"/>

            <!-- Middle -->
            <Button x:Name="MiddleLeft" Grid.Row="1" Grid.Column="0" FontSize="32" Click="Cell_Click"/>
            <Button x:Name="MiddleCenter" Grid.Row="1" Grid.Column="1" FontSize="32" Click="Cell_Click"/>
            <Button x:Name="MiddleRight" Grid.Row="1" Grid.Column="2" FontSize="32" Click="Cell_Click"/>

            <!-- Bottom -->
            <Button x:Name="BottomLeft" Grid.Row="2" Grid.Column="0" FontSize="32" Click="Cell_Click"/>
            <Button x:Name="BottomCenter" Grid.Row="2" Grid.Column="1" FontSize="32" Click="Cell_Click"/>
            <Button x:Name="BottomRight" Grid.Row="2" Grid.Column="2" FontSize="32" Click="Cell_Click"/>
        </Grid>
        
        <!-- Status bar -->
        <Label x:Name="StatusBar" FontSize="14" Grid.Row="2" Margin="10">
        </Label>
        
    </Grid>
</Window>
